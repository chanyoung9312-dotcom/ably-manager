'use client';
import{useState}from'react';
import * as XLSX from'xlsx';

const phone=v=>String(v||'').replace(/[^0-9]/g,'').replace(/^(01\d)(\d{3,4})(\d{4})$/,'$1-$2-$3');
const text=v=>String(v??'').trim();
const DEFAULT_TEMPLATE=`안녕하세요 {수취인명} 고객님

고객님께서 주문하신 {상품명} 상품은 저희 주문량이 밀려 저희 업체로 공급되는 시간이 지체되어 곧바로 배송이 어려워 안내드립니다.

배송 기간은 영업일 기준으로 3일~7일 정도 소요될 수도 있는 것으로 확인됩니다. 배송현황은 CJ대한통운으로 등록되어있을텐데 실제 배송은 우체국택배로 진행됩니다!

문자 확인 후 답변 해주시면 배송 진행, 혹은 취소 진행으로 도와드리겠습니다

감사합니다!!`;
const POST_HEADERS=['받는 분 이름','받는 분 우편번호','받는 분 주소(도로명주소+건물번호, 읍/면/동까지)','받는분 상세주소(나머지주소\n / 상세주소가 없는 경우 입력 예시 : . , 등)','받는 분 연락처','물품중량(kg)','물품크기(cm)\n크기=가로+세로+높이','분할접수여부','첫번째 중량(kg)','첫번째 크기(cm)','두번째 중량(kg)','두번째 크기(cm)','내용품코드','내용물','배달방식','배송시특이사항','보내는분 이름','보내는분 우편번호','보내는분 주소(도로명주소+건물번호, 읍/면/동까지)','보내는분 상세주소(나머지주소)','보내는분 연락처'];
const FIXED=['5','80','N','11','80','11','80','[25]의류/패션잡화','의류','비대면','','오어즈','48224','부산광역시 수영구 망미번영로74번길 43-4','수정 1층 102호','010-5943-7724'];
function splitAddress(v){const s=text(v).replace(/\s+/g,' ');const m=s.match(/^(.+?(?:로|길)\s*\d+(?:-\d+)?)(?:\s+(.*))?$/);if(m)return[m[1],m[2]||'.'];const parts=s.split(' ');if(parts.length>4)return[parts.slice(0,4).join(' '),parts.slice(4).join(' ')||'.'];return[s,'.'];}
function parseSmsSheet(wb){const ws=wb.Sheets[wb.SheetNames[0]],raw=XLSX.utils.sheet_to_json(ws,{defval:''});return raw.filter(r=>text(r['상품주문번호']||r['주문번호']||r['상품명'])).map((r,i)=>({id:text(r['상품주문번호'])||`${text(r['주문번호'])}-${i}`,name:text(r['수취인명']||r['주문자명']),customer:text(r['주문자명']||r['수취인명']),phone:phone(r['수취인 연락처']||r['연락처']),product:text(r['상품명']),option:text(r['옵션 정보']),qty:text(r['수량'])||'1',copied:false}));}
function parsePostSheet(wb){const ws=wb.Sheets['에이블리 주문']||wb.Sheets[wb.SheetNames[0]],aoa=XLSX.utils.sheet_to_json(ws,{header:1,defval:'',raw:false});if(!aoa.length)return[];const out=[],seen=new Set();for(let i=1;i<aoa.length;i++){const r=aoa[i];if(text(r[7])!=='우체국 배송')continue;const orderNo=text(r[10]||r[4]),name=text(r[26]),mobile=phone(r[27]),zip=text(r[28]),address=text(r[29]),memo=text(r[30]);if(!name||!address)continue;const key=[orderNo,name,mobile,zip,address].join('|');if(seen.has(key))continue;seen.add(key);const[a,d]=splitAddress(address);out.push({id:key,orderNo,name,phone:mobile,zip,address:a,detail:d,memo});}return out;}
export default function Page(){
 const[mode,setMode]=useState('sms'),[rows,setRows]=useState([]),[postRows,setPostRows]=useState([]),[fileName,setFileName]=useState(''),[notice,setNotice]=useState(''),[template,setTemplate]=useState(DEFAULT_TEMPLATE);
 async function loadFile(e,type){const file=e.target.files?.[0];if(!file)return;try{const wb=XLSX.read(await file.arrayBuffer(),{type:'array'});if(type==='post'){const p=parsePostSheet(wb);setPostRows(p);setNotice(`배송관리 = 우체국 배송 ${p.length}건을 찾았습니다.`);}else{const p=parseSmsSheet(wb);setRows(p);setNotice(`${p.length}건을 불러왔습니다.`);}setFileName(file.name);}catch{setNotice('엑셀을 읽지 못했습니다. 파일 형식을 확인해주세요.');}}
 function message(r){return template.replaceAll('{수취인명}',r.name||'고객').replaceAll('{고객명}',r.name||'고객').replaceAll('{주문자명}',r.customer||'고객').replaceAll('{상품명}',r.product||'주문 상품').replaceAll('{옵션}',r.option||'').replaceAll('{수량}',r.qty||'1');}
 async function copy(r){await navigator.clipboard.writeText(message(r));setRows(v=>v.map(x=>x.id===r.id?{...x,copied:true}:x));}
 function downloadPost(){if(!postRows.length)return;const top=['받는 분 정보','','','','','물품정보(*필수)','','','','','','','','','','','보내는 분 정보(*선택)','','','',''];const data=postRows.map(r=>[r.name,r.zip,r.address,r.detail||'.',r.phone,...FIXED]);const ws=XLSX.utils.aoa_to_sheet([top,POST_HEADERS,...data]);ws['!merges']=[XLSX.utils.decode_range('A1:E1'),XLSX.utils.decode_range('F1:P1'),XLSX.utils.decode_range('Q1:U1')];ws['!cols']=[18,14,42,32,18,12,14,12,14,14,14,14,22,14,14,24,16,14,42,24,18].map(w=>({wch:w}));const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'방문접수');XLSX.writeFile(wb,`우체국_등록용_${new Date().toISOString().slice(0,10)}.xlsx`);}
 return <main className="wrap"><h1>Ably Manager</h1><p className="sub">문자 생성과 우체국 발송 준비를 한 곳에서 처리해요.</p><div className="actions" style={{marginBottom:18}}><button onClick={()=>setMode('sms')} style={{opacity:mode==='sms'?1:.45}}>문자 도우미</button><button onClick={()=>setMode('post')} style={{opacity:mode==='post'?1:.45}}>우체국 엑셀 만들기</button></div>
 {mode==='sms'?<><section className="card"><h2>1. 에이블리 엑셀 올리기</h2><input type="file" accept=".xlsx,.xls" onChange={e=>loadFile(e,'sms')}/>{notice&&<p className="sub">{fileName&&`${fileName} · `}{notice}</p>}</section><section className="card"><h2>2. 문자 양식</h2><p className="sub">{'{수취인명} {상품명} {옵션} {수량}'}을 사용할 수 있어요.</p><textarea value={template} onChange={e=>setTemplate(e.target.value)} style={{width:'100%',minHeight:300,padding:12,border:'1px solid #ddd',borderRadius:10,font:'inherit',lineHeight:1.6}}/></section><div className="row"><h2>문자 보내기</h2><b>{rows.length}건</b></div>{rows.length===0?<div className="card">엑셀을 올리면 여기에 고객별 문자가 표시됩니다.</div>:rows.map(r=><article className="card" key={r.id}><div className="row"><div><b>{r.name}</b> <span className="sub">{r.phone}</span></div><span>{r.copied?'복사 완료':'대기'}</span></div><p><b>{r.product}</b>{r.option?` · ${r.option}`:''} · {r.qty}개</p><div className="msg">{message(r)}</div><div className="actions"><button onClick={()=>copy(r)}>{r.copied?'다시 복사':'문자 복사'}</button>{r.phone&&<button onClick={()=>navigator.clipboard.writeText(r.phone)}>번호 복사</button>}</div></article>)}</>:<><section className="card"><h2>1. 주문시트 올리기</h2><p className="sub">에이블리 주문 시트의 H열 배송관리가 정확히 ‘우체국 배송’인 주문만 가져옵니다.</p><input type="file" accept=".xlsx,.xls" onChange={e=>loadFile(e,'post')}/>{notice&&<p className="sub">{fileName&&`${fileName} · `}{notice}</p>}</section><div className="row"><h2>발송 대상</h2><b>{postRows.length}건</b></div>{postRows.length>0&&<section className="card"><button onClick={downloadPost}>우체국 등록용 엑셀 다운로드</button><p className="sub">중복된 같은 주문은 한 번만 만들고, 우체국 양식의 고정값은 자동으로 채웁니다.</p></section>}{postRows.length===0?<div className="card">주문시트를 올리면 우체국 배송 대상이 표시됩니다.</div>:postRows.map(r=><article className="card" key={r.id}><div className="row"><b>{r.name}</b><span>{r.phone}</span></div><p>{r.zip} · {r.address} {r.detail}</p><p className="sub">주문번호 {r.orderNo}</p></article>)}</>}</main>;
}