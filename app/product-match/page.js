'use client';
import {useState} from 'react';
import * as XLSX from 'xlsx';
const t=v=>String(v??'').trim();
export default function ProductMatch(){const[files,setFiles]=useState([]),[goods,setGoods]=useState([]),[loading,setLoading]=useState(false),[result,setResult]=useState(null),[error,setError]=useState('');async function load(e){const fs=[...(e.target.files||[])];setFiles(fs);setResult(null);setError('');try{const all=[];for(const f of fs){const w=XLSX.read(await f.arrayBuffer(),{type:'array'}),s=w.Sheets[w.SheetNames[0]],rows=XLSX.utils.sheet_to_json(s,{defval:'',raw:false});for(const r of rows)all.push({productNo:t(r['상품 번호']),name:t(r['상품명']),category:t(r['카테고리']),registeredAt:t(r['상품등록일'])})}const unique=[...new Map(all.filter(x=>x.productNo&&x.name).map(x=>[x.productNo,x])).values()];setGoods(unique)}catch{setGoods([]);setError('상품목록 CSV를 읽지 못했습니다.')}}async function run(){if(!goods.length)return;setLoading(true);setError('');try{const r=await fetch('/api/md-product-match',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({goods})}),d=await r.json();if(!r.ok)throw new Error(d.error);setResult(d)}catch(e){setError(e.message)}finally{setLoading(false)}}const box={background:'#181a1c',border:'1px solid #34383d',borderRadius:18,padding:20};return <main style={{maxWidth:1000,margin:'auto',padding:'28px 18px 70px',fontFamily:'sans-serif',color:'#f4f4f5'}}><a href="/" style={{color:'#a1a1aa',textDecoration:'none'}}>← OARS Manager</a><h1 style={{marginBottom:6}}>상품번호 자동 매칭</h1><p style={{color:'#a1a1aa',marginTop:0}}>에이블리 상품목록 CSV와 MD 시트 상품명을 비교해 MD 시트의 ‘상품번호’ 칸을 자동으로 채웁니다.</p><section style={{...box,marginTop:20}}><h2 style={{marginTop:0}}>1. 에이블리 상품목록 CSV 선택</h2><input type="file" accept=".csv" multiple onChange={load}/><p style={{color:'#a1a1aa'}}>여러 CSV를 한 번에 선택해도 됩니다. 옵션 중복은 상품번호 기준으로 자동 제거합니다.</p>{files.length>0&&<div><b>선택 파일 {files.length}개</b> · 고유 상품 {goods.length}개</div>}</section><section style={{...box,marginTop:14}}><h2 style={{marginTop:0}}>2. MD 시트와 매칭</h2><p style={{color:'#a1a1aa'}}>상품명이 정확히 일치하거나 충분히 확실한 경우만 자동 입력합니다. 애매한 상품은 억지로 채우지 않고 ‘확인 필요’로 남깁니다.</p><button onClick={run} disabled={loading||!goods.length}>{loading?'매칭 중...':'상품번호 자동 입력'}</button>{error&&<p style={{color:'#fb7185'}}>{error}</p>}</section>{result&&<><section style={{...box,marginTop:14}}><h2 style={{marginTop:0}}>매칭 결과</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))',gap:10}}>{[['MD 상품',result.total],['새로 입력',result.written],['기존값 유지',result.existing],['확인 필요',result.needsReview]].map(([a,b])=><div key={a} style={{background:'#111315',padding:14,borderRadius:12}}><small style={{color:'#a1a1aa'}}>{a}</small><div style={{fontSize:25,fontWeight:800,marginTop:4}}>{b}개</div></div>)}</div></section>{result.needsReview>0&&<section style={{...box,marginTop:14}}><h2 style={{marginTop:0}}>확인 필요</h2>{result.results.filter(x=>x.status==='확인 필요').slice(0,30).map((x,i)=><div key={i} style={{padding:'10px 0',borderBottom:'1px solid #2b2f33'}}><b>{x.product}</b>{x.candidate&&<div style={{color:'#a1a1aa',marginTop:4}}>가장 가까운 에이블리 상품: {x.candidate}</div>}</div>)}</section>}</>}</main>}
